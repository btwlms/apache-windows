# apache-windows
git clone https://github.com/rajeswari1203/apache-windows.git
unzip apche24.zip to C drive
Run chef-solo -c solo.rb
httpd cookbook runs apache
Apachemoniter.rb is optional
After running apache it will reboot system.

